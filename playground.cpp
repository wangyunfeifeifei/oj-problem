#include <iostream>
#include <string>
#include <algorithm>
#include <sstream>
#include <vector>
#include <map>

using namespace std;

int maxTrailingZeros(vector<vector<int>>& grid) {
    int maxZero=0;
    int m = grid.size();
    int n = grid[0].size();
    for (int i = 0; i < m; i++) {

    }
}

int main() {

    return 0;
}